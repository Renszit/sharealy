//compiler
//font api /google
//use image
//use artist
// send to people (may-be a different component)

export default function Compiler() {
    return (
        <div>
            <h1>compiler</h1>
        </div>
    );
}
